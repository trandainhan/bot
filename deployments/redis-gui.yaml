---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-commander
  namespace: bot
  annotations:
    # Tell Kubernetes to apply the AppArmor profile "runtime/default".
    # Note that this is ignored if the Kubernetes node is not running version 1.4 or greater.
    # and fails if AppArmor enabled but profile not found (may happens on borked k8s installs only)
    # set to "unconfined" to disable AppArmor
    container.apparmor.security.beta.kubernetes.io/redis-commander: runtime/default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-commander
  template:
    metadata:
      labels:
        app: redis-commander
        tier: backend
    spec:
      containers:
        - name: redis-commander
          image: rediscommander/redis-commander
          imagePullPolicy: IfNotPresent
          env:
            - name: REDIS_HOSTS
              value: "instance1:redis-coingiatot-prod-master.redis-prod:6379:1"
            - name: K8S_SIGTERM
              value: "1"
            - name: HTTP_USER
              value: "superbot"
            - name: HTTP_PASSWORD
              value: "HD+9,mt549Z)_t9_"
          ports:
            - name: redis-commander
              containerPort: 8081
          livenessProbe:
            httpGet:
              path: /favicon.png
              port: 8081
            initialDelaySeconds: 10
            timeoutSeconds: 5
          # adapt to your needs base on data stored inside redis (number of keys and size of biggest keys)
          # or comment out for less secure installation
          resources:
            limits:
              cpu: "500m"
              memory: "300M"
            requests:
              cpu: "100m"
              memory: "128M"
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      nodeSelector:
        lifecycle: Ec2Mixed
        name: phoenix
